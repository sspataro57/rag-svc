apiVersion: v1
kind: ConfigMap
metadata:
  name: rag-svc-config
data:
  # ---- per-instance Redis isolation ----
  REDIS_DB: "1"
  REDIS_KEY_PREFIX: "rag-tes:"

  # No Atlassian envs — TES corpus is reconstructed and personal-project
  # summaries are fed via POST /ingest/jira and POST /ingest/document.
