apiVersion: v1
kind: Service
metadata:
  name: rag-svc-lan
  labels:
    app: rag-svc
spec:
  type: NodePort
  selector:
    app: rag-svc
  ports:
    - name: http
      port: 8080
      targetPort: http
      nodePort: 30181
      protocol: TCP
